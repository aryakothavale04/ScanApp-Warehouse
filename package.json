{
  "name": "scanapp-packing-pwa",
  "private": true,
  "version": "1.0.0",
  "description": "Mobile-first PWA for wholesale grocery packing with invoice upload, barcode scanning, and MongoDB order tracking.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "seed": "npm run seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
